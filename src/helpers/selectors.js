export function getAppointmentsForDay(state, day) {
  //... returns an array of appointments for that day

  //1find object in state.says array that === the provided day
  //2with that provided days appointment array
  //itterate through and compare id and id states.appointment return value
  //3!appointment on that provide day return = []
}